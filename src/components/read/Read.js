import React, {Fragment} from 'react';

const Read = () => {
    return (
        <Fragment>
            <p className="white">Read front app here</p>
        </Fragment>
    );
};

export default Read;
